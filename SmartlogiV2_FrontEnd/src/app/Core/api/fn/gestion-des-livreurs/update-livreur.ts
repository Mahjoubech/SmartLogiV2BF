/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { LivreurRequest } from '../../models/livreur-request';
import { LivreurResponse } from '../../models/livreur-response';

export interface UpdateLivreur$Params {
  livreur_id: string;
      body: LivreurRequest
}

export function updateLivreur(http: HttpClient, rootUrl: string, params: UpdateLivreur$Params, context?: HttpContext): Observable<StrictHttpResponse<LivreurResponse>> {
  const rb = new RequestBuilder(rootUrl, updateLivreur.PATH, 'put');
  if (params) {
    rb.path('livreur_id', params.livreur_id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'blob', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<LivreurResponse>;
    })
  );
}

updateLivreur.PATH = '/api/v1/gestionner/livreur/{livreur_id}';
