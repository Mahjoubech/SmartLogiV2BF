/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PageLivreurResponse } from '../../models/page-livreur-response';

export interface GetAllLivreurs$Params {
  page?: number;
  size?: number;
  sortBy?: string;
  sortDir?: string;
}

export function getAllLivreurs(http: HttpClient, rootUrl: string, params?: GetAllLivreurs$Params, context?: HttpContext): Observable<StrictHttpResponse<PageLivreurResponse>> {
  const rb = new RequestBuilder(rootUrl, getAllLivreurs.PATH, 'get');
  if (params) {
    rb.query('page', params.page, {});
    rb.query('size', params.size, {});
    rb.query('sortBy', params.sortBy, {});
    rb.query('sortDir', params.sortDir, {});
  }

  return http.request(
    rb.build({ responseType: 'blob', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PageLivreurResponse>;
    })
  );
}

getAllLivreurs.PATH = '/api/v1/gestionner/livreur';
